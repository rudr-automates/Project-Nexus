{
  "name": "nexus-scraper",
  "version": "1.0.0",
  "private": true,
  "description": "Project Nexus shared browser-automation service. Wraps Browserless and exposes a stable REST API so n8n (and any future workflow) can orchestrate browser automation without running Puppeteer/Playwright/Chromium inside n8n itself.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer-core": "^22.15.0"
  }
}
